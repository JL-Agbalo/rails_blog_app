<%= form_with model: @article, local: true do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation" style="color: red; background: #ffcccc; border: 1px solid red; padding: 10px; margin-bottom: 20px;">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>
      <ul>
        <% @article.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>